namespace Silo.Application;
/// <summary>Mirrors the document type categories supported by the Silo AI RAG API.</summary>
public enum RagDocType
{
    GeneralChat,
    Report,
    Image,
    PageAgent
}
